[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
description = "A sample test package"

authors = [
	{name = "jcologne", email = "jcologne@student.42sp.org.br"}
]

readme = "README.md"
license = "MIT"

[project.urls]
Homepage = "https://github.com/jocologne/Python_0.git"